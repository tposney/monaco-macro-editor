
    export default [
      "typescript\\lib\\lib.es2017.string.d.ts",
      "/// <reference no-default-lib=\"true\"/>\r\ninterface String{/**\r * Pads the current string with a given string (possibly repeated) so that the resulting string reaches a given length.\r * The padding is applied from the start (left) of the current string.\r *\r * @param maxLength The length of the resulting string once the current string has been padded.\r *        If this parameter is smaller than the current string's length, the current string will be returned as it is.\r *\r * @param fillString The string to pad the current string with.\r *        If this string is too long, it will be truncated and the left-most part will be applied.\r *        The default value for this parameter is \" \" (U+0020).\r */\npadStart(maxLength:number,fillString?:string):string;/**\r * Pads the current string with a given string (possibly repeated) so that the resulting string reaches a given length.\r * The padding is applied from the end (right) of the current string.\r *\r * @param maxLength The length of the resulting string once the current string has been padded.\r *        If this parameter is smaller than the current string's length, the current string will be returned as it is.\r *\r * @param fillString The string to pad the current string with.\r *        If this string is too long, it will be truncated and the left-most part will be applied.\r *        The default value for this parameter is \" \" (U+0020).\r */\npadEnd(maxLength:number,fillString?:string):string;}"
    ]
  